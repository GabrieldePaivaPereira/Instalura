import {StyleSheet} from "react-native";

const style = StyleSheet.create({
    fotoUsuario: {
      width:40, 
      height:40,
      borderRadius:30,
      margin: 10
    },
    cabecalho: {
        flexDirection: "row",
        alignItems: "center"
    }
})

export default style;